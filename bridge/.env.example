# Gmail OAuth (can reuse the same Google project — add this redirect URI)
GMAIL_OAUTH_REDIRECT=http://127.0.0.1:3535/gmail/callback

# WhatsApp (Twilio) — from a WhatsApp-enabled sender
TWILIO_WA_FROM=whatsapp:+1xxxxxxxxxx
